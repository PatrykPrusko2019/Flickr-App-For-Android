<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SearchActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:theme="@style/AppTheme.AppBarOverlay">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay" />


        <Button
            android:id="@+id/buttonResult"
            android:layout_width="66dp"
            android:layout_height="34dp"
            android:layout_toRightOf="@+id/value_search"
            android:background="@android:drawable/ic_menu_search"
            android:onClick="onClickedButtonResult"
            android:shadowColor="@color/md_blue_grey_500"
            android:textSize="12sp" />

        <EditText
            android:id="@+id/value_search"
            android:layout_width="279dp"
            android:layout_height="44dp"
            android:layout_marginLeft="35dp"
            android:layout_marginRight="8dp"
            android:hint="@string/enter_the_search_word"
            android:inputType="text"
            android:gravity="center"
            tools:text="enter the search word" />

    </RelativeLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>